{{- if .Values.hpa.enabled }}
apiVersion: {{ .Values.hpa.apiVersion | default "autoscaling/v1" }}
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "foodie.componentFullname" (dict "context" . "component" "frontend") }}
  namespace: {{ include "foodie.namespace" . }}
  labels:
    {{- include "foodie.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "foodie.componentFullname" (dict "context" . "component" "frontend") }}
  minReplicas: {{ .Values.hpa.frontend.minReplicas }}
  maxReplicas: {{ .Values.hpa.frontend.maxReplicas }}
  {{- if eq (.Values.hpa.apiVersion | default "autoscaling/v1") "autoscaling/v2" }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.frontend.targetCPUUtilizationPercentage }}
  {{- else }}
  targetCPUUtilizationPercentage: {{ .Values.hpa.frontend.targetCPUUtilizationPercentage }}
  {{- end }}
{{- end }}
